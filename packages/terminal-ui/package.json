{
  "name": "@devx-crew/terminal-ui",
  "version": "0.1.0",
  "description": "Shared terminal presentation for DevX Crew tools",
  "license": "MIT",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": ["dist"],
  "dependencies": {
    "@devx-crew/reviewer": "0.1.0"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "node --test --import=tsx 'src/**/*.test.ts'",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "tsx": "^4.20.0"
  }
}
