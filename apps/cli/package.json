{
  "name": "@devx-mux/cli",
  "version": "0.1.0",
  "description": "DevX Mux command-line interface",
  "license": "MIT",
  "type": "module",
  "bin": {
    "mux": "dist/main.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "node --test --import=tsx 'src/**/*.test.ts'",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@devx-mux/reviewer": "0.1.0",
    "@devx-mux/terminal-ui": "0.1.0",
    "bun": "^1.3.14"
  },
  "devDependencies": {
    "tsx": "^4.20.0"
  }
}
