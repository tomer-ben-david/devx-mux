interface:
  display_name: "Staged PR Review"
  short_description: "Run gated multi-stage pull request reviews"
  default_prompt: "Use $staged-pr-review to run each pull request review stage to a clean result before advancing."
