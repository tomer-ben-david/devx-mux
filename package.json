{
  "name": "devx-mux",
  "private": true,
  "version": "0.1.0",
  "description": "Multiplex independent AI implementation and review workflows.",
  "license": "MIT",
  "type": "module",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "tsc -b packages/terminal-ui packages/reviewer apps/cli",
    "mux": "node scripts/mux.ts",
    "cli": "node scripts/cli.ts",
    "test": "npm run test --workspaces --if-present && npm run test:skills",
    "test:skills": "node --test --import=tsx 'skills/**/*.test.ts'",
    "typecheck": "tsc -b packages/terminal-ui packages/reviewer apps/cli && tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^5.9.0"
  },
  "engines": {
    "node": ">=22"
  },
  "allowScripts": {
    "bun@1.3.14": true
  }
}
